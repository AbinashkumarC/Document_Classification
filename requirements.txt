# -----------------------------------------------
# DocScan AI — Document Classification Service
# Requirements File
# Install with: pip install -r requirements.txt
# -----------------------------------------------

# ---------- Web Framework ----------
Flask==3.1.3
Werkzeug==3.1.7          # Comes with Flask, pinned for secure_filename

# ---------- PDF Processing ----------
pdf2image==1.17.0         # Converts PDF pages to images (needs poppler installed)
Pillow==12.1.1            # Image processing (PIL)

# ---------- OCR ----------
pytesseract==0.3.13       # Python wrapper for Tesseract OCR engine
                          # Also install: sudo apt install tesseract-ocr

# ---------- Fuzzy Matching ----------
rapidfuzz==3.14.5         # Fast fuzzy string matching

# ---------- Timezone ----------
pytz==2026.1.post1        # Timezone support (IST)

# ---------- Database ----------
psycopg2-binary==2.9.12   # PostgreSQL adapter (binary = no build needed)

# ---------- (No extra install needed — stdlib) ----------
# smtplib    — built-in, email sending
# threading  — built-in, background tasks
# concurrent.futures — built-in, thread pool
# tempfile   — built-in, temp directory
# logging    — built-in
# re, os, json, time — built-ingunicorn
gunicorn
